# Tatacoa Verde

Pequeño sitio estático optimizado para producción.

Qué incluye:
- HTML limpio
- CSS con responsive básico (mobile/tablet/desktop)
- JS encapsulado y mínimo para la vista detalle

Cómo probar localmente:

1. Abrir `index.html` en el navegador.

Listo para subir a GitHub (añadir repo y push).